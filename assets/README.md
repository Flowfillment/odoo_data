# Brand assets

Place the company logo here as `logo.png` (or `.jpg`/`.svg`) — the
customer-report generator embeds `assets/logo.*` automatically in the
report banner and generates without a logo when the file is absent.
Source: the Van Thiel United brandbook.
